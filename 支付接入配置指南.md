# CrystalMuse 支付接入配置指南

## 架构概览

```
用户浏览器 (前端)                    支付服务器 (后端)                    支付网关
┌─────────────┐    ┌──────────────────────────┐    ┌─────────────┐
│  填写信息    │───▶│  POST /api/create-       │───▶│   Stripe    │
│  选择支付    │    │  checkout-session        │    │   Checkout  │
│  方式       │    │                          │    └──────┬──────┘
│             │◀───│  返回 Stripe Checkout URL │◀──────────┘
│  跳转支付页  │    └──────────────────────────┘
│             │                                       ┌─────────────┐
│  输入卡号    │─────────────────────────────────────▶│   Stripe    │
│  完成支付    │                                       │   付款页面   │
│             │◀─────── 跳转回 /payment-success ──────┤             │
└─────────────┘                                       └─────────────┘

                      ┌──────────────────────────┐    ┌─────────────┐
│  选择 PayPal │───▶│  POST /api/create-        │───▶│   PayPal    │
│             │    │  paypal-order             │    │   API       │
│             │◀───│  返回 PayPal Order ID     │◀───┤             │
│  弹出 PayPal │    └──────────────────────────┘    └──────┬──────┘
│  登录授权    │                                          │
│             │    ┌──────────────────────────┐    ┌──────▼──────┐
│             │───▶│  POST /api/capture-      │───▶│   PayPal    │
│             │    │  paypal-order            │    │   扣款      │
│             │◀───│  确认成功                │◀───┤             │
│  跳转成功页  │    └──────────────────────────┘    └─────────────┘
└─────────────┘
```

**支付流程：**
1. **Stripe (信用卡/Apple Pay/Google Pay)**：前端 → 后端创建 Checkout Session → 跳转 Stripe 托管支付页 → 支付完成跳回成功页 → Webhook 确认
2. **PayPal**：前端 → 后端创建 PayPal Order → 前端弹出 PayPal 授权窗口 → 授权后后端扣款 → 跳转成功页

---

## 一、Stripe 接入

### 1.1 注册 Stripe 账号

1. 访问 https://dashboard.stripe.com/register
2. 填写邮箱、密码、国家/地区
3. 完成邮箱验证
4. 填写商户信息（公司名称、地址、银行账户等）
5. **注意**：中国大陆个人/企业也可注册 Stripe，但需要海外银行账户收款。推荐使用：
   - Wise (https://wise.com) — 提供虚拟美元/欧元账户
   - Payoneer (https://payoneer.com) — 支持中国用户
   - Airwallex (https://airwallex.com) — 支持中国企业

### 1.2 获取 API 密钥

1. 登录 Stripe Dashboard: https://dashboard.stripe.com
2. 左侧菜单 → **Developers** → **API Keys**
3. 复制以下两个密钥：
   - **Publishable key**（公钥）：以 `pk_test_` 或 `pk_live_` 开头
   - **Secret key**（私钥）：以 `sk_test_` 或 `sk_live_` 开头

### 1.3 配置环境变量

编辑 `server/.env` 文件：

```env
STRIPE_SECRET_KEY=sk_test_51XXXXXXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_PUBLISHABLE_KEY=pk_test_51XXXXXXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXX
```

### 1.4 配置 Webhook（生产环境必须）

Webhook 用于接收 Stripe 的支付状态通知，确保即使用户关闭浏览器，订单状态也能更新。

1. Stripe Dashboard → **Developers** → **Webhooks** → **Add endpoint**
2. Endpoint URL: `https://yourdomain.com/api/stripe-webhook`
3. 监听以下事件：
   - `checkout.session.completed` — 支付成功
   - `checkout.session.expired` — 会话过期
   - `payment_intent.payment_failed` — 支付失败
4. 创建后复制 **Signing secret**（`whsec_` 开头）
5. 填入 `.env` 文件的 `STRIPE_WEBHOOK_SECRET`

**本地开发测试 Webhook：**
```bash
# 安装 Stripe CLI
# 下载: https://github.com/stripe/stripe-cli/releases

# 登录
stripe login

# 转发 webhook 到本地
stripe listen --forward-to localhost:8080/api/stripe-webhook

# 会输出类似: Ready! Your webhook signing secret is whsec_xxxxx
# 将这个 secret 填入 .env
```

### 1.5 测试信用卡

在 Stripe 测试模式下，使用以下测试卡号：

| 卡号 | 场景 | 结果 |
|------|------|------|
| `4242 4242 4242 4242` | 正常支付 | ✅ 成功 |
| `4000 0027 6000 3184` | 需要 3D 验证 | ✅ 成功（需验证） |
| `4000 0000 0000 9995` | 余额不足 | ❌ 失败 |
| `4000 0000 0000 0002` | 卡被拒绝 | ❌ 失败 |

- 过期日期：任意未来日期，如 `12/34`
- CVC：任意 3 位数字，如 `123`
- 邮编：任意，如 `12345`

### 1.6 启用 Apple Pay / Google Pay

1. Stripe Dashboard → **Settings** → **Payment methods**
2. 启用 Apple Pay 和 Google Pay
3. **Apple Pay 需要**：在 Stripe Dashboard 注册你的域名并上传验证文件
4. 生产环境中，Stripe Checkout 页面会自动检测设备并显示 Apple Pay / Google Pay 按钮

---

## 二、PayPal 接入

### 2.1 注册 PayPal Developer 账号

1. 访问 https://developer.paypal.com
2. 使用现有 PayPal 账号登录，或注册新账号
3. 进入 **Dashboard** → **My Apps & Credentials**

### 2.2 创建 REST API 应用

**Sandbox（测试环境）：**
1. 在 Sandbox 标签下点击 **Create App**
2. App Name: `CrystalMuse`
3. App Type: **Merchant**（商户）
4. 创建后获取：
   - **Client ID** — 客户端 ID
   - **Secret** — 密钥

**Live（生产环境）：**
1. 切换到 Live 标签
2. 同样创建 App
3. 获取生产环境的 Client ID 和 Secret

### 2.3 配置环境变量

编辑 `server/.env` 文件：

```env
# Sandbox（测试）
PAYPAL_CLIENT_ID=ATXXXXXXXXXXXXXXXXXXXXXXXX
PAYPAL_CLIENT_SECRET=EXXXXXXXXXXXXXXXXXXXXXXXXX
PAYPAL_MODE=sandbox

# Live（生产）— 切换时修改
# PAYPAL_CLIENT_ID=AXXXXXXXXXXXXXXXXXXXXXXXX
# PAYPAL_CLIENT_SECRET=EXXXXXXXXXXXXXXXXXXXXXXXXX
# PAYPAL_MODE=live
```

### 2.4 测试 PayPal

**Sandbox 测试账号：**
1. PayPal Developer Dashboard → **Testing** → **Sandbox Accounts**
2. 使用 Personal 类型的测试账号登录 PayPal 进行支付测试
3. 测试账号格式：`buyer@example.com` / 密码在创建时设置

---

## 三、启动支付服务器

### 3.1 环境准备

```bash
# 1. 确认 Node.js 已安装（v18+）
node --version

# 2. 进入项目目录
cd crystal-store

# 3. 确认 .env 文件已配置
# 如果没有配置任何密钥，服务器仍可启动（演示模式）
# 前端会显示提示信息并模拟支付流程
```

### 3.2 启动命令

```bash
# 设置 NODE_PATH 指向 node_modules（因为依赖安装在全局 workspace）
# Windows (Git Bash):
NODE_PATH="C:/Users/Admin/.workbuddy/binaries/node/workspace/node_modules" \
  node server/server.js

# Linux/Mac:
NODE_PATH="./node_modules" node server/server.js

# 如果在项目目录下安装了依赖：
node server/server.js
```

### 3.3 验证服务器

```bash
# 检查健康状态
curl http://localhost:8080/api/health

# 预期输出：
# {
#   "status": "ok",
#   "stripe": "configured" 或 "not configured",
#   "paypal": "configured" 或 "not configured",
#   "paypalMode": "sandbox"
# }
```

---

## 四、支付流程详解

### 4.1 Stripe 支付流程

```
1. 用户在结算页填写收货信息
2. 选择"Credit Card"或"Apple Pay / Google Pay"
3. 点击"Place Order"
4. 前端 POST /api/create-checkout-session
   → 发送: { items: [...], customer: {...} }
   ← 返回: { url: "https://checkout.stripe.com/c/xxx" }
5. 前端跳转到 Stripe Checkout 页面
6. 用户在 Stripe 页面输入卡号 / 使用 Apple Pay
7. 支付成功 → Stripe 跳转回 /payment-success.html?session_id=xxx
8. Stripe 同时发送 Webhook → /api/stripe-webhook
9. 成功页请求 /api/session/xxx 获取订单详情并展示
```

### 4.2 PayPal 支付流程

```
1. 用户在结算页填写收货信息
2. 选择"PayPal"
3. 点击"Place Order"
4. 前端 POST /api/create-paypal-order
   → 发送: { items: [...], customer: {...} }
   ← 返回: { paypalOrderId: "xxx", orderId: "CMxxx" }
5. 前端动态加载 PayPal JS SDK
6. 渲染 PayPal 按钮，用户点击后弹出 PayPal 授权窗口
7. 用户登录 PayPal 并授权
8. 前端 POST /api/capture-paypal-order
   → 发送: { paypalOrderId: "xxx", orderId: "CMxxx" }
   ← 返回: { status: "success" }
9. 前端跳转到 /payment-success.html?provider=paypal&order_id=xxx
```

---

## 五、上线检查清单

### Stripe 上线

- [ ] Stripe Dashboard 完成商户验证（KYC）
- [ ] 绑定银行账户（Wise / Payoneer / Airwallex）
- [ ] 将 API 密钥从 `sk_test_` 切换到 `sk_live_`
- [ ] 将公钥从 `pk_test_` 切换到 `pk_live_`
- [ ] 配置生产环境 Webhook URL（HTTPS）
- [ ] 设置 Webhook Signing Secret
- [ ] 测试一笔真实小额交易
- [ ] 启用 Apple Pay 并完成域名验证
- [ ] 确认支持的支付方式（Visa/Mastercard/Amex/Discover）

### PayPal 上线

- [ ] 拥有 PayPal Business 账号
- [ ] 完成账号验证（邮箱、银行卡、身份）
- [ ] 创建 Live REST API App
- [ ] 将 Client ID / Secret 切换到 Live 版本
- [ ] 将 `PAYPAL_MODE` 改为 `live`
- [ ] 测试一笔真实小额交易
- [ ] 配置 PayPal Webhook（可选但推荐）

### 通用上线

- [ ] 域名配置 HTTPS（SSL 证书）
- [ ] 设置 `DOMAIN` 环境变量为生产域名
- [ ] 确认服务器使用 PM2 / forever 等进程管理器
- [ ] 配置 Nginx 反向代理（如果需要）
- [ ] 设置订单数据备份
- [ ] 实现订单确认邮件发送（可接入 SendGrid / Mailgun）
- [ ] 实现 1688 供应商自动下单通知

---

## 六、安全注意事项

1. **永远不要在前端暴露 Secret Key** — 只有 Publishable Key 可以在前端使用
2. **Webhook 必须验证签名** — 生产环境必须设置 `STRIPE_WEBHOOK_SECRET`
3. **使用 HTTPS** — 生产环境必须使用 SSL，Stripe 和 PayPal 都要求 HTTPS
4. **不要在代码仓库中提交 .env 文件** — 已在 .gitignore 中排除
5. **定期轮换 API 密钥** — 建议每 3-6 个月更换一次
6. **监控异常交易** — 在 Stripe Dashboard 设置欺诈检测规则
7. **PCI 合规** — 使用 Stripe Checkout 意味着你不需要自己处理卡号数据，大幅降低 PCI 合规要求

---

## 七、文件结构

```
crystal-store/
├── index.html              # 前端主页面
├── payment-success.html    # 支付成功页面
├── checkout-cancel.html    # 支付取消页面
├── css/
│   └── style.css           # 样式文件
├── js/
│   ├── data.js             # 产品数据
│   └── app.js              # 应用逻辑（含支付集成）
├── server/
│   ├── server.js           # 支付服务器（Express + Stripe + PayPal）
│   ├── .env                # 环境变量（需自行填写密钥）
│   ├── .env.example        # 环境变量模板
│   └── orders.json         # 订单数据存储（自动生成）
└── 支付接入配置指南.md       # 本文档
```

---

## 八、常见问题

### Q: 服务器启动后显示 "Stripe not configured"？
A: 检查 `server/.env` 文件中的 `STRIPE_SECRET_KEY` 是否已填写。确保 `.env` 文件与 `server.js` 在同一目录。

### Q: Stripe 支付成功但没有收到 Webhook？
A: 
- 本地开发：使用 Stripe CLI (`stripe listen --forward-to localhost:8080/api/stripe-webhook`)
- 生产环境：检查 Webhook URL 是否正确、是否为 HTTPS

### Q: PayPal 按钮不显示？
A: 检查 `PAYPAL_CLIENT_ID` 是否正确，浏览器控制台是否有错误。PayPal SDK 从 PayPal 服务器动态加载。

### Q: 如何切换到生产模式？
A: 
1. Stripe: 将 `.env` 中的 `sk_test_` → `sk_live_`，`pk_test_` → `pk_live_`
2. PayPal: 将 Sandbox 凭证替换为 Live 凭证，`PAYPAL_MODE=live`
3. 重启服务器

### Q: 订单数据存在哪里？
A: 订单保存在 `server/orders.json` 文件中。生产环境建议迁移到数据库（MySQL / MongoDB / PostgreSQL）。

### Q: 如何对接 1688 供应商自动下单？
A: 支付成功后（Webhook 中），可以：
1. 调用 1688 开放 API 自动下单
2. 发送邮件给供应商人工下单
3. 通过钉钉/企业微信机器人通知
详见 `供应商方案与运营指南.md`。
