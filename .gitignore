# Dependencies
node_modules/
package-lock.json

# Environment / secrets
.env
server/.env

# Logs
*.log
logs/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Temporary scripts
_*.js

# Runtime data (Render free tier is ephemeral; keep local copies untracked)
server/orders.json
server/messages.json
server/reviews.json
server/users.json
server/subscribers.json
server/refund-requests.json
server/abandoned-carts.json

# Uploaded images (product photos, review photos) — runtime data
uploads/
server/uploads/

# Local audit/dev artifacts (may contain secrets)
full-audit.js

# Generated WebP images — deployed via SFTP, not tracked in git (avoid repo bloat)
images/**/*.webp
images/*.webp

# Sitemap mirrors — regenerated on server startup alongside sitemap.xml
sitemap-google.xml
sitemap-v3.xml
